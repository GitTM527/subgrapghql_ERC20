{
  "name": "erc20token-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ erc20token-subgraph",
    "create-local": "graph create --node http://localhost:8020/ erc20token-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ erc20token-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 erc20token-subgraph",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.85.0",
    "@graphprotocol/graph-ts": "^0.35.1",
    "matchstick-as": "0.5.0"
  }
}
